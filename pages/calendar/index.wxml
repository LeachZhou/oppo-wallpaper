<view>
  <view class="loading" wx:if="{{loading}}"></view>
  <view wx:else>
    <view wx:for="{{allResList}}" wx:key="index">
      <view class='calendar-line'>{{item.time}}</view>
      <view class='img-list'>
        <image mode="aspectFill" src='{{itemImg.img}}' wx:for="{{item.data}}" wx:for-item="itemImg" wx:key="index" style="width: {{width}}px;height: {{height}}px;"></image>
      </view>
    </view>
  </view>
</view>